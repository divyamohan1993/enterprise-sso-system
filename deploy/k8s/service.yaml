apiVersion: v1
kind: Service
metadata:
  name: milnet-sso
  labels:
    app: milnet-sso
spec:
  type: ClusterIP
  selector:
    app: milnet-sso
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
