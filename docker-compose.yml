version: '3.8'

services:
  sso-admin:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ADMIN_PORT=8080
      - RUST_LOG=info
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/health"]
      interval: 10s
      timeout: 5s
      retries: 3
